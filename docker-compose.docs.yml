services:
  docs:
    image: django_prod_shop_local_docs
    container_name: django_prod_shop_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - /app/.venv
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./django_prod_shop:/app/django_prod_shop:z
    ports:
      - '9000:9000'
    command: /start-docs
